// FOUNDATION FALLBACK — replaced by piece `typeface-lettering` via registerFaces().
// Deliberately plain: system italic 900, no outlines, no bevel, no gradient.
import { fallbackFaces } from '../typeface.js';
export default fallbackFaces;
