// PIECE: touch-controller — NOT YET BUILT.
// The foundation fallback owns this slot until this piece registers a real one.
// See README.md "THE SPINE" for the contract and the harness command that judges it.
export default null;
